RSpec.describe Underscore::String::Rails do
  it "has a version number" do
    expect(Underscore::String::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
